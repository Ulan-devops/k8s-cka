apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: nginx-allows-busybox-to-access
spec:
  podSelector:
    matchLabels:
      tier: backend
  ingress:
  - from:
    - podSelector:
        matchLabels:
          tier: frontend
    